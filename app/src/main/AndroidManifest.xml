<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.n01351778.carlos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DenyListProject">
        <activity
            android:name=".AboutActivity"
            android:label="@string/title_activity_about"
            android:theme="@style/Theme.DenyListProject.NoActionBar"></activity>
        <activity
            android:name=".HelpActivity"
            android:label="@string/title_activity_help"
            android:theme="@style/Theme.DenyListProject.NoActionBar" />
        <activity
            android:name=".ShowingAllActivity"
            android:label="@string/title_activity_showing_all"
            android:theme="@style/Theme.DenyListProject.NoActionBar" />
        <activity
            android:name=".ConfirmationActivity"
            android:label="@string/title_activity_confirmation"
            android:theme="@style/Theme.DenyListProject.NoActionBar" />
        <activity
            android:name=".AddingActivity"
            android:label="@string/title_activity_adding"
            android:theme="@style/Theme.DenyListProject.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>